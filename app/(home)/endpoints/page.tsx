import React from "react";

const Endpoints = () => {
  return (
    <div className="flex items-center justify-center h-screen">
      <h1 className="text-5xl font-bold">Endpoints</h1>
    </div>
  );
};

export default Endpoints;
